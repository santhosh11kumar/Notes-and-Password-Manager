/// <reference types="node" />
interface IConfig {
    algorithm?: string;
    encryptionKey?: string;
    salt?: string;
    iv?: Buffer;
}
export default class Encryption {
    private algorithm;
    private key;
    private salt;
    private iv;
    constructor(config: IConfig);
    encrypt: (value?: string | number | undefined, isInt?: boolean) => string;
    decrypt: (token?: string | undefined, isInt?: boolean) => string;
}
export {};
